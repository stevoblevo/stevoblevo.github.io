/* Anewgam: The Nougat Thread. Optional scene; no host or account authority. */
(function (root, factory) {
  const api = factory();
  if (typeof module === 'object' && module.exports) module.exports = api;
  else root.AnewgamNougat = api;
})(typeof globalThis === 'object' ? globalThis : this, function () {
  'use strict';
  const SCHEMA = 'anewgam.nougat.v1';
  const TYPES = ['weave', 'light', 'gift', 'handoff', 'cross', 'share', 'rest'];
  const NAMES = {weave:'Mend the stair',light:'Wake the lanterns',gift:'Leave a nougat',handoff:'Pass the thread',cross:'Follow her footsteps',share:'Share the nougat',rest:'Keep a place warm',skip:'Leave things as they are'};
  const API = 'https://simple-jev-demo-api.featherless.ai/v1';
  const initial = () => ({role:'princess',woven:false,lit:false,gift:false,crossed:false,ended:false});
  const clone = x => JSON.parse(JSON.stringify(x));
  function allowed(s) {
    if (s.ended) return [];
    if (s.role === 'princess') return [...(!s.woven?['weave']:[]),...(!s.lit?['light']:[]),...(!s.gift?['gift']:[]),...((s.woven||s.lit)?['handoff']:[])];
    return !s.crossed ? ['cross'] : [s.gift?'share':'rest'];
  }
  function transition(s,type) {
    if (!TYPES.includes(type) || !allowed(s).includes(type)) throw new Error('That action does not belong to this moment.');
    const n = {...s};
    if (type==='weave') n.woven=true;
    if (type==='light') n.lit=true;
    if (type==='gift') n.gift=true;
    if (type==='handoff') n.role='knight';
    if (type==='cross') n.crossed=true;
    if (type==='share'||type==='rest') n.ended=true;
    return n;
  }
  function replay(events) {
    if (!Array.isArray(events)||events.length>7) throw new Error('Invalid scene thread.');
    return events.reduce((s,event)=>{
      if (!event || Object.keys(event).length!==1 || typeof event.type!=='string') throw new Error('Invalid scene event.');
      return transition(s,event.type);
    }, initial());
  }
  function unpack(raw) {
    if (typeof raw!=='string'||raw.length>32768) throw new Error('Scene file is too large.');
    const x=JSON.parse(raw);
    if (!x||x.schema!==SCHEMA||Object.keys(x).sort().join(',')!=='events,schema') throw new Error('Not a Nougat Thread scene file.');
    replay(x.events); return clone(x.events);
  }
  const pack = events => { replay(events); return {schema:SCHEMA,events:clone(events)}; };
  function requestFor(s,model) {
    if (typeof model!=='string'||!model||model.length>150) throw new Error('No valid model was returned.');
    const choices=[...allowed(s),'skip'];
    if (choices.length<2) throw new Error('This little story is already complete.');
    return {model,state:{scene:'Fictional Anewgam story. No real account, person, device or payment.',...s},questions:{next:{type:'choice',instructions:'Suggest one available story action. Leave a gift before handing over the thread. Make a path if there is none. Never invent an action. A suggestion is not an action.',criteria:Object.fromEntries(choices.map(k=>[k,NAMES[k]]))}}};
  }
  function validateAdvice(request,response) {
    const options=Object.keys(request.questions.next.criteria), a=response?.answers?.next;
    if (response?.model!==request.model || a?.type!=='choice' || !options.includes(a.choice)) throw new Error('The specialist returned an unexpected choice.');
    const p=a.probabilities;
    if (!p||Object.keys(p).sort().join('|')!==[...options].sort().join('|')||Object.values(p).some(v=>typeof v!=='number'||!Number.isFinite(v)||v<0||v>1)) throw new Error('The specialist returned invalid scores.');
    const total=Object.values(p).reduce((x,y)=>x+y,0);
    if (Math.abs(total-1)>0.001||p[a.choice]!==Math.max(...Object.values(p))) throw new Error('The specialist scores disagree with its choice.');
    return {choice:a.choice,score:p[a.choice],model:response.model};
  }
  function mount(container,options={}) {
    if (!container||typeof container.attachShadow!=='function') throw new Error('A scene container is required.');
    const shadow=container.attachShadow({mode:'open'}), host=options.host||null;
    let events=[],s=initial(),revision=0,proposal=null,busy=false,controller=null,remoteResult=null,disposed=false;
    let storage='This scene is in this tab. Keep or export it when you choose.';
    if (host) { try { const saved=host.getState().nougatScene; if (saved) {events=unpack(JSON.stringify(saved));s=replay(events);storage='Resumed from the existing cockpit. Other threads are untouched.';} } catch {storage='The saved scene could not be read. Your original data was not changed.';} }
    shadow.innerHTML=`<style>
      :host{display:block;color:#f7eedb;font:16px/1.5 system-ui,sans-serif;--gold:#f0cf84;--panel:#141d29;--dim:#b9b9bb}*{box-sizing:border-box}button,a,input{font:inherit}button,a{touch-action:manipulation}button{cursor:pointer;border:1px solid #63513c;color:#f7eedb;background:#252631;border-radius:10px;padding:11px 15px;transition:background .15s,transform .15s}button:hover:not(:disabled){background:#3c3540;transform:translateY(-1px)}button:disabled{cursor:default;opacity:.45}button:focus-visible,a:focus-visible,summary:focus-visible{outline:3px solid #efc978;outline-offset:4px}.wrap{max-width:1450px;margin:auto;background:#111721;border:1px solid #413a33;border-radius:20px;overflow:hidden;box-shadow:0 25px 80px #0006}.mast{display:flex;justify-content:space-between;align-items:center;padding:18px 28px;border-bottom:1px solid #3f3933;gap:16px}.brand{font:26px Georgia,serif;letter-spacing:.06em;color:var(--gold)}.tiny{font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:var(--dim)}.body{display:grid;grid-template-columns:minmax(0,1.17fr) minmax(360px,1fr)}.world{position:relative;min-height:660px;isolation:isolate;background:#242027;overflow:hidden}.art{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;object-position:46% 27%;opacity:.66;transition:object-position 1s,filter 1s,opacity 1s;filter:saturate(.65)}.world.knight .art{object-position:26% 100%}.world.ended .art{opacity:.85;filter:saturate(1)}.shade{position:absolute;inset:0;background:linear-gradient(180deg,#12172130,#11172110 30%,#111721e8 97%)}.over{position:absolute;inset:0;display:flex;flex-direction:column;justify-content:space-between;padding:30px}.scene-top{display:flex;justify-content:space-between;gap:10px}.seal{border:1px solid #aa885b85;border-radius:50px;padding:7px 13px;background:#181c25af;font-size:12px}.sky-note{font:italic 20px/1.5 Georgia,serif;max-width:220px;color:#f7e4b9}.trail{position:absolute;left:13%;top:43%;width:80%;height:33%;pointer-events:none;opacity:.2;transition:opacity 1s}.world.woven .trail{opacity:1}.lantern{position:absolute;top:26%;right:15%;font-size:52px;filter:grayscale(1);opacity:.3;transition:opacity 1s,filter 1s}.world.lit .lantern{opacity:1;filter:drop-shadow(0 0 25px #efb955)}.gift{align-self:flex-start;background:#252330dc;border:1px solid #be9b6380;padding:9px 15px;border-radius:30px;color:var(--gold);font-size:14px}.world h2{font:clamp(36px,4vw,57px)/1.05 Georgia,serif;margin:0 0 15px;max-width:470px;text-shadow:0 2px 15px #000}.world p{max-width:430px;font:italic 18px/1.5 Georgia,serif;color:#e0d3b9}.side{padding:30px 34px;display:flex;flex-direction:column;gap:18px}.chapter{color:var(--gold);font-size:11px;text-transform:uppercase;letter-spacing:.16em}.side h1{font:38px/1.1 Georgia,serif;margin:4px 0 12px}.story{color:#d9d6d0;min-height:78px;margin:0}.steps{display:flex;gap:5px}.steps span{height:3px;background:#30313a;flex:1}.steps .on{background:var(--gold)}.actions{display:grid;gap:9px}.action{text-align:left;display:grid;grid-template-columns:38px 1fr;align-items:center;min-height:65px;background:#1b2430}.action .icon{font-size:24px;color:var(--gold)}.action b{font-size:15px;font-weight:600}.action small{display:block;color:#aaaeb4;font-size:12px}.primary{background:#e6c483;color:#1d2330;border-color:#e6c483}.primary:hover:not(:disabled){background:#f4d99e}.primary small{color:#464647}.line{height:1px;background:#39332e}.thread{display:flex;flex-wrap:wrap;gap:6px;min-height:26px}.thread span{border:1px solid #4b463d;background:#1b2430;border-radius:7px;padding:5px 9px;font-size:11px;color:#ddd0b7}.status{font-size:12px;color:#c8c6c1;min-height:38px;margin:0}.status.error{color:#ffbda9}.tools{display:flex;flex-wrap:wrap;gap:7px}.tools button{font-size:12px;padding:8px 10px}.remote{border:1px solid #424354;padding:13px;border-radius:12px;background:#1b2230}.remote button{font-size:13px}.remote small{display:block;color:#c7c6c2;font-size:11px;margin:8px 0}.advice{font-size:13px;margin:10px 0;color:#f1d49d}.foot{display:flex;justify-content:space-between;gap:15px;padding:14px 28px;background:#0c131c;border-top:1px solid #353039;font-size:11px;color:#aaadb3}.foot a{color:#cfba8b}summary{cursor:pointer;font-size:12px;color:#d6c5a1}pre{white-space:pre-wrap;overflow-wrap:anywhere;font-size:10px;max-height:160px;overflow:auto;color:#b9bcc4}.hidden,[hidden]{display:none!important}.victory{font:italic 21px Georgia,serif;color:var(--gold);padding:16px;border:1px solid #8c704980;border-radius:10px}.badge{padding:7px 10px;color:#d5e5d7;background:#193029;border:1px solid #476350;border-radius:20px;font-size:11px}.hint-row{display:flex;gap:8px;align-items:center;flex-wrap:wrap}@media(max-width:800px){.body{grid-template-columns:1fr}.world{min-height:380px;max-height:430px}.art{object-position:50% 35%}.side{padding:24px}.mast{padding:14px 20px}.world h2{font-size:39px}.sky-note{display:none}.foot{flex-direction:column;gap:5px;padding:13px 20px}}@media(prefers-reduced-motion:reduce){*{transition:none!important;animation:none!important}}
    </style><div class="wrap"><header class="mast"><div class="brand">Anewgam <span class="tiny">/ a little playable chapter</span></div><span class="badge">No sign-in · offline play</span></header><div class="body"><section class="world" aria-label="The world your choices change"><img class="art" alt="Princess and Knight connected by a golden thread through an unfinished world."><div class="shade"></div><svg class="trail" viewBox="0 0 500 230" aria-hidden="true"><path d="M5 218 Q90 100 193 149 T490 18" fill="none" stroke="#f0cf84" stroke-width="3" stroke-dasharray="8 7"/></svg><span class="lantern" aria-hidden="true">✦</span><div class="over"><div class="scene-top"><span class="seal" id="role"></span><span class="tiny">2187 / story, not host status</span></div><p class="sky-note">Not a world already finished.<br>A world made by what you do.</p><div><div class="gift" id="gift"></div><h2 id="world-title"></h2><p id="world-line"></p></div></div></section><main class="side"><div><div class="chapter" id="chapter"></div><h1>The Nougat Thread</h1><p class="story" id="story" aria-live="polite"></p></div><div class="steps" aria-hidden="true"><span></span><span></span><span></span></div><div class="actions" id="actions"></div><div class="victory" id="ending" hidden></div><div><div class="chapter">The world remembers</div><div class="thread" id="thread"></div></div><section class="remote" id="remote" hidden><div class="hint-row"><button id="ask">Ask a free specialist</button><button id="cancel" hidden>Cancel</button><span class="tiny">SimpleJev / hosted</span></div><small>Optional: this click sends only the fictional scene flags and available actions to Featherless. Not your image, account, or other cockpit threads. Advice never acts by itself.</small><p class="advice" id="advice">Play freely without AI. A specialist can suggest one next move.</p><button id="apply" class="primary" hidden>Try this suggestion</button><details><summary>See the request before sending</summary><pre id="request"></pre></details></section><div class="line"></div><p class="status" id="status" aria-live="polite"></p><div class="tools"><button id="keep">Keep this thread</button><button id="export">Export story</button><button id="import">Import story</button><button id="undo">Undo one stitch</button><button id="reset">New thread</button><input id="file" type="file" accept="application/json,.json" hidden></div></main></div><footer class="foot"><span>Scene state only · no host commands · no money, posting, or account access</span><span><a href="https://stevoblevo.github.io/anewgam-for-steven-cockpit/" target="_blank" rel="noopener noreferrer">Existing cockpit ↗</a> · <a href="https://aethernet-the-warmth.stevoblevo.chatgpt.site/" target="_blank" rel="noopener noreferrer">The Warmth ↗</a></span></footer></div>`;
    const $=sel=>shadow.querySelector(sel);
    const art=options.art;
    if(typeof art==='string' && (/^data:image\/(webp|png|jpeg);base64,/.test(art)||/^\.?\.?\/[\w/.-]+\.(webp|png|jpg)$/.test(art))) $('.art').src=art;
    else $('.art').hidden=true;
    function say(text,error=false){$('#status').textContent=text;$('#status').classList.toggle('error',error);}
    function cancel(){if(controller)controller.abort();controller=null;busy=false;proposal=null;revision++;$('#apply').hidden=true;$('#cancel').hidden=true;}
    function narrative(){
      if(s.ended) return s.gift?['A little kindness, inherited.','He finds the sweet you left and breaks it in two. Not a reward from nowhere: a kindness carried forward.','One nougat. Two places.','You made the path. You left the gift. He found both.']:['A place kept warm.','No parcel waited here. He leaves a seat beside the lantern instead. A different beginning, not a failed one.','The story keeps a place.','Next time, a small gift might cross with the thread.'];
      if(s.role==='knight') return !s.crossed?['The Knight arrives later.',`${s.woven?'The broken stair is mended.':'The stair is still broken.'} ${s.lit?'Her lanterns mark a way.':'A gold seam leads across the stone.'} ${s.gift?'A wrapped nougat waits by the door.':'No gift was left here.'} These are your earlier choices, not a reset world.`,'Someone has been here.','Princess discovers. Knight inherits.']:['A trace becomes a meeting.',s.gift?'The path has brought him to the parcel. Shall he keep it, or make room for two?':'He has crossed by the path she left. He sets a place beside the fire.','Yesterday’s choice. Today’s world.','This world remembers what happened.'];
      return ['The Princess finds an unfinished stair.','A stair, a lantern, and a little parcel: choose what to make real before the Knight arrives. You need one way across. The rest is yours.','Play creates the world.','A single stitch can become someone else’s road.'];
    }
    function render(){
      const n=narrative();$('#role').textContent=s.role==='princess'?'I · Princess discovers':'II · Knight inherits';$('#chapter').textContent=s.ended?'III / a world that remembers':n[0];$('#story').textContent=n[1];$('#world-title').textContent=n[2];$('#world-line').textContent=n[3];$('.world').className='world '+['woven','lit','ended'].filter(k=>s[k]).join(' ')+(s.role==='knight'?' knight':'');$('#gift').textContent=s.gift?'✧ A nougat has been left for later.':'✦ A small kindness is still possible.';
      [...shadow.querySelectorAll('.steps span')].forEach((x,i)=>x.classList.toggle('on',i<=(s.ended?2:s.role==='knight'?1:0)));
      $('#actions').replaceChildren();
      const small={weave:'The broken stone remembers a golden seam.',light:'A second path appears in the dark.',gift:'Someone who comes later may find it.',handoff:'Let the Knight inherit precisely what you made.',cross:s.woven?'Cross by the stair you repaired.':'Follow the lantern path you awakened.',share:'A very small feast for two.',rest:'A quiet ending; another thread can begin.'};
      for(const type of allowed(s)) {const b=document.createElement('button');b.className='action'+(['handoff','cross','share','rest'].includes(type)?' primary':'');b.dataset.action=type;const icon=document.createElement('span');icon.className='icon';icon.textContent=({weave:'⌁',light:'✦',gift:'◇',handoff:'→',cross:'↗',share:'♡',rest:'◌'})[type];const body=document.createElement('span'),title=document.createElement('b'),sub=document.createElement('small');title.textContent=NAMES[type];sub.textContent=small[type];body.append(title,sub);b.append(icon,body);b.onclick=()=>act(type);$('#actions').append(b);}
      $('#thread').replaceChildren();for(const e of events){const tag=document.createElement('span');tag.textContent=NAMES[e.type];$('#thread').append(tag);}if(!events.length){const tag=document.createElement('span');tag.textContent='No footsteps yet.';$('#thread').append(tag);}
      $('#ending').hidden=!s.ended;$('#ending').textContent=s.gift?'Nougat earned. The gift reached the next player.':'The seat is warm. The next story is unwritten.';$('#undo').disabled=!events.length;$('#keep').disabled=!events.length;$('#export').disabled=!events.length;$('#ask').disabled=busy||s.ended;$('#cancel').hidden=!busy;$('#remote').hidden=!options.allowRemote;
      $('#request').textContent=JSON.stringify(s.ended?{status:'story_complete'}:requestFor(s,'(discovered from the public model list on click)'),null,2);say(storage);
    }
    function act(type){try {const next=transition(s,type);cancel();events.push({type});s=next;remoteResult=null;$('#advice').textContent='The scene changed. Ask again for a fresh suggestion.';storage='A real scene change. Nothing sent; keep or export it when you choose.';render();}catch(e){say(e.message,true);}}
    $('#undo').onclick=()=>{cancel();events.pop();s=replay(events);storage='One stitch undone. Other cockpit threads are untouched.';render();};
    $('#reset').onclick=()=>{cancel();events=[];s=initial();storage='A fresh thread in this tab. Your kept copy has not been erased.';render();};
    $('#keep').onclick=()=>{try{if(host){host.setState({...host.getState(),nougatScene:pack(events)});storage='Keep requested through the existing cockpit; its storage warning is authoritative.';}else{localStorage.setItem(SCHEMA,JSON.stringify(pack(events)));storage='Kept on this browser. No network request.';}say(storage);}catch{say('Storage is unavailable. Export the story to keep it.',true);}};
    $('#export').onclick=()=>{const blob=new Blob([JSON.stringify(pack(events),null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Anewgam_Nougat_Thread.story.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);say('Exported only this story’s action list. No other cockpit data included.');};
    $('#import').onclick=()=>$('#file').click();$('#file').onchange=async()=>{const file=$('#file').files[0];$('#file').value='';if(!file)return;try{if(file.size>32768)throw new Error('Scene file is too large.');const loaded=unpack(await file.text());cancel();events=loaded;s=replay(events);storage='Imported scene replayed. Nothing applied outside this chapter.';render();}catch{say('That file is not a valid story. Your current scene is unchanged.',true);}};
    $('#cancel').onclick=()=>{cancel();render();$('#advice').textContent='Request cancelled here; a provider may already have processed it. No move was made.';};
    async function getJSON(url,init){const response=await fetch(url,{...init,credentials:'omit'});if(!response.ok)throw new Error('HTTP '+response.status+' from the public demo. Manual play still works.');const text=await response.text();if(text.length>100000)throw new Error('Unexpectedly large provider response.');return JSON.parse(text);}
    $('#ask').onclick=async()=>{if(busy||s.ended||!options.allowRemote)return;busy=true;proposal=null;$('#apply').hidden=true;const startedRevision=revision, snapshot=clone(s),ac=new AbortController();controller=ac;const timeout=setTimeout(()=>ac.abort(),15000);render();$('#advice').textContent='Asking the hosted specialist. No move is being made.';try{const models=await getJSON(API+'/models',{signal:ac.signal});const ids=(models.data||[]).map(x=>x.id).filter(x=>typeof x==='string'&&x.length<=150);const model=ids.find(x=>/qwen/i.test(x))||ids.find(x=>/gemma/i.test(x))||ids[0];const request=requestFor(snapshot,model);const response=await getJSON(API+'/classifier',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(request),signal:ac.signal});const result=validateAdvice(request,response);if(disposed||startedRevision!==revision||ac.signal.aborted)return;proposal={...result,revision};remoteResult={request,response};$('#advice').textContent='Suggested: '+NAMES[result.choice]+'. Model preference: '+(100*result.score).toFixed(1)+'% — not certainty. '+result.model;$('#apply').hidden=result.choice==='skip';}catch(e){if(!disposed&&startedRevision===revision){$('#advice').textContent=ac.signal.aborted?'The request timed out or was cancelled. No move was made.':'No live suggestion: '+(e instanceof Error?e.message:'connection unavailable');}}finally{clearTimeout(timeout);if(!disposed&&startedRevision===revision){busy=false;controller=null;$('#ask').disabled=s.ended;$('#cancel').hidden=true;}}};
    $('#apply').onclick=()=>{if(!proposal||proposal.revision!==revision)return say('That suggestion is stale. No move made.',true);act(proposal.choice);};
    if(!host){try{const kept=localStorage.getItem(SCHEMA);if(kept){events=unpack(kept);s=replay(events);storage='Resumed your kept story on this browser.';}}catch{storage='Storage unavailable or saved story unreadable; original data was not changed.';}}
    render();
    return {getState:()=>clone(s),getEvents:()=>clone(events),getRemoteResult:()=>remoteResult&&clone(remoteResult),act,destroy:()=>{disposed=true;cancel();shadow.replaceChildren();}};
  }
  return Object.freeze({SCHEMA,initial,allowed,transition,replay,pack,unpack,requestFor,validateAdvice,mount});
});
