# Anewgam / Goober: source and build receipts

This is a working first slice, not a claim that all of Saelion is connected.

## Recovered, preserved, linked
Three Library artworks appear in the Sources view. Original bytes remain downloadable. Display crops and WebP compression are separate files with SHA-256 values and transformation recipes in /assets/asset-provenance.json. The generated imagery is concept art, not proof of a running game. Baked-in HUD text in an original is not live application state.

The prior Crossing screenshot mentions three videos and a scene manifest. Library searches did not recover those bytes. The titles and reported asset IDs are retained in the trail with reported-not-recovered status. No video is advertised as playable.

## Play and continuity
Main: / with World, Field log, Sources and Install views. Three short authored story steps share one local log. Outrider, Artificer and Wayfinder are optional new fictional approaches, not fixed identities or permission grants.
Generic: /saedow/ is a reusable field log with a separate browser database, sharing the exact SaeDow 0.1 record format. Its Signal Run is a local momentum toy, not a competitive score service or Rocket League training claim.

Records are device/browser-profile-local IndexedDB data, not an authenticated private cloud vault. They are unencrypted. Another person using that profile can read them. A completed status is self-reported. Writing a request does not dispatch work. Export before clearing site data. Import requires an empty destination and a valid complete chain; no automatic cross-device merging or synchronization is present.

## Evidence and limits
/receipts/trail.json is a portable, hash-linked source trail. SHA-256 and predecessor links allow integrity checks against received evidence; someone able to replace the entire chain can recompute it. There is no signature, identity attestation, or live Skein acceptance implied.

No account enrollment, live Sae/AI chat, Steam link, messaging, Roku control, home-system authority, Saedo worker, or remote installation was performed. The app's Install view gives real browser steps. Physical iPhone and Windows Edge installation remains to be verified by use on those devices.

The site uses no server-side app database, analytics SDK, or custom advertising tracker. Its local field logs use the device/browser-profile IndexedDB described above. The previously wired Peachfall heat beacon was removed from the shipped page; the hosting platform may still keep normal access/error logs. No family contact details, accident details, email addresses, or account credentials are seeded in the public app.

Source provenance is embedded in the site; source/build exports and verification reports supplement this packet. Deployment is recorded separately only after the hosting tool confirms it.

`/goober/receipts/archive/89185c985927/static-file-hashes.json` belongs to the historical release pinned by `/goober-release.json`; it is not a manifest of the current deployed tree. The manual archive importer verifies the manifest copy inside that downloaded archive before it can replace a reviewed import branch.

## Tower story thread

`/#tower` is an additive story-mode seam in the existing Goober PWA. It reuses
the already-public `peachfall-09` concept and links a chosen path, seam, or tool
to the existing project form and SaeDow field log. Opening or choosing does not
save; the player must review the candidate and explicitly choose Keep. The log
remains device-local, unencrypted, authority-none, and unsubmitted to Skein.

The public world-law and source hashes are recorded at
`/goober/source/anewgam-world-thread.v1.json`. The separately supplied operator
context is deliberately excluded from public assets and receipts.

The seven public Skein-knot keeper entries now carry the complete Atlas shape
and visible wall tiles. The route resolver’s exact allowlist matches every
declared public route. The checked-in tree is the publication source; CI only
verifies it and cannot restore an older archive or push generated files.

## Take 3 and PC setup continuation
The main world now includes a Take 3 media section. Direct retrieval of the supplied Grok asset returned HTTP403 and zero bytes. Original link and click-to-try playback are retained. A visitor can select a local MP4 (maximum 100 MiB); it is validated, hashed and stored only in that browser. A local file is NOT automatically considered identical to the blocked original. The poster is existing Crossing artwork, not a recovered frame. Browser tests used synthetic MP4 data.

The PC workbench at /#setup (navigation aliases /$, /> and /setup) provides a reversible dual-boot preparation guide. Bazzite KDE NVIDIA desktop is the proposal to evaluate; Omarchy remains an option. No exact current disk layout or ISO has been accepted. No Windows, bootloader, encryption, firmware, or account mutation was performed. Setup records are local draft requests, not authority or verified installation reports. Bird/Blubird is an optional imagined sketch.

34 unit tests and 20 Take3/setup browser checks passed on the local compiled Worker harness, with zero JavaScript runtime errors. This does not verify an iPhone installation or the original source video.

The base deployment was reported successful by the provider, but independent anonymous requests returned HTTP401 at the hosting boundary. Deployment status and public delivery must remain separate until anonymous access is actually verified. The site was not listed on a community feed.
